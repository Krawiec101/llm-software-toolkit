# Copy this file to project.yaml in the target project when local defaults are useful.
project:
  name: example-project
  type: web-application

paths:
  frontend: frontend
  backend: backend
  tests: tests

commands:
  install: ""
  lint: ""
  test: ""
  build: ""

agent_instructions:
  codex:
    - AGENTS.md
    - agent.md
  claude:
    - CLAUDE.md

conventions:
  language: ""
  framework: ""
  architecture: ""
  testing: ""
  notes:
    - Keep assumptions explicit when project context is missing.
