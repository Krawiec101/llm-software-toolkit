---
name: symfony-project-bootstrap
description: Bootstrap or minimally extend the Symfony-specific base of a PHP REST API. Use for stable Symfony setup, PHP compatibility, namespace, routing and dependency injection only.
---

# Symfony Project Bootstrap

Read and obey `references/php-api-skill-contract.md`. Complete `tooling-research` for the current official stable bootstrap path, Symfony release and compatible PHP version.

Implement only the base project, namespace, routing and baseline dependency injection. Do not configure Docker, tests, quality tools, HTTP logging or OpenAPI. Preserve compatible existing Symfony structure on reruns.

Completion criterion: the approved stable Symfony base boots with its declared PHP version and no responsibility owned by another skill was added.
