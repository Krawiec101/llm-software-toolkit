---
name: laravel-project-bootstrap
description: Bootstrap or minimally extend the Laravel-specific base of a PHP REST API. Use for stable Laravel setup, PHP compatibility, namespace, routing and container configuration only.
---

# Laravel Project Bootstrap

Read and obey `references/php-api-skill-contract.md`. Complete `tooling-research` for the current official stable bootstrap path, Laravel release and compatible PHP version.

Implement only the base project, namespace, routing and baseline service-container configuration. Do not configure Docker, tests, quality tools, HTTP logging or OpenAPI. Preserve compatible existing Laravel structure on reruns.

Completion criterion: the approved stable Laravel base boots with its declared PHP version and no responsibility owned by another skill was added.
